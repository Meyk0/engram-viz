# @engramviz/studio

The prebuilt standalone local Studio used by the [Engram CLI](https://github.com/Meyk0/engram-viz).

Most developers should use `@engramviz/cli` rather than starting this package directly. The package contains a production Next.js server and Engram's static brain assets; no source checkout is required.
