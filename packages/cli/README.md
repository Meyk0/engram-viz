# @engramviz/cli

The local-first CLI for [Engram](https://github.com/Meyk0/engram-viz).

Initialize capture with `engram init`, launch the packaged workbench with `engram dev`, inject local capture variables with `engram run`, and execute exported memory regressions with `engram test`.
