# @engramviz/sdk

The TypeScript capture SDK for [Engram](https://github.com/Meyk0/engram-viz), an open-source memory reliability workbench for AI agents.

Wrap agent turns with `EngramClient.withTurn` and record observable store, retrieve, load, update, supersede, delete, and summarize operations. Capture is fail-open by default.
